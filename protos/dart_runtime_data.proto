syntax = "proto3";

package globe_runtime;

message DartMessage {
  bool done = 2;

    // Using `oneof` to make `data` and `error` nullable
  oneof payload {
    bytes data = 3;
    string error = 4;
  }
}
